# python package requirements file
# note which revision of python, for example 3.9.6


# Core Google ADK
google-adk>=1.12.0

# MCP support (required by google-adk MCP tools)
mcp>=1.2.0

# Google Gemini SDK (pulled transitively, pinned for safety)
google-generativeai>=0.8.3

# Node-based MCP servers are invoked externally (npx)
# Ensure Node.js >=18 is installed in the environment


typing

python-dotenv>=1.0.1